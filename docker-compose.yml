---

x-common: &common
  build:
    target: development
    context: .
    dockerfile: Dockerfile-dev
  image: "cs-wayback-machine:dev"
#  env_file: .env
  volumes:
    - .:/opt/code

services:
  devtools:
    <<: *common
    command: sleep infinity
    ports:
      - "8000:8000"

  app:
    <<: *common
    restart: "no"
    ports:
      - "8000:8000"
